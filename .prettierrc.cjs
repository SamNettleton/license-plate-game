module.exports = {
  // Use semi-colons at the end of statements
  semi: true,
  // Use single quotes instead of double quotes (cleaner for TS)
  singleQuote: true,
  // Ensure trailing commas where possible (makes git diffs much cleaner)
  trailingComma: 'all',
  // Standard line width before wrapping
  printWidth: 100,
  // How many spaces per indentation level
  tabWidth: 2,
  // Put the > of a multi-line HTML element at the end of the last line
  bracketSameLine: false,
  // Include parentheses around a sole arrow function parameter
  arrowParens: 'always',
  // End of line character (set to 'lf' for cross-platform compatibility)
  endOfLine: 'lf',
};