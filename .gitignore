# Personal editor settings
.vscode/
*.code-workspace

# --- Python (Backend) ---
# Ignores pycache and compiled files anywhere in the project
**/__pycache__/
**/*.py[cod]
# Ignores the virtual environment folder
**/venv/
**/.venv/

# --- JavaScript/TypeScript (Frontend) ---
# Ignores the massive dependencies folder
**/node_modules/
# Ignores the production build folder (we build this in Docker instead)
**/dist/

# --- Environment & Secrets ---
# NEVER commit these
**/.env
**/.env.local
